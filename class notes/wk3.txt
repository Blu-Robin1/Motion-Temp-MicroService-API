synchronies communication
	signal gets sent, the receiver communicates yes i received it, then the code resume execution

asynchronies communication
	sent and resume without waiting for acknowledgment, need to make sure that the signals that were sents still be received after system boot up 


Single monolithic db, easy to handle, all the services tightly couple and can become slow
db per service, decouple, disadvantage is consistence load

ORM will create a row into a table, the mapping is done by that

in python you write a class in python class, ORM will convert it to MySQL language